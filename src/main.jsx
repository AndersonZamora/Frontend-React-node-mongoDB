import React from 'react';
import ReactDOM from 'react-dom/client';
import { AndersonApp } from './AndersonApp';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AndersonApp />
  </React.StrictMode>
)
